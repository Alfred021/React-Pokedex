import '../app-css/loading.css';

const Loading = () => {
    return (
        <section className="loading">
            <h1>Loading...</h1>
            <div className="spin"></div>
        </section>
    )
}

export default Loading;
